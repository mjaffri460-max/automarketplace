I'm a non-technical founder and this is the frontend of my project.

What it is: people that aren’t able to access their dream car nearby them in there region. Allowing them to specifically order online and get their dream car shipped. Also make sure that it’s simple for people like  elders to  be able to use this and younger people to easily get there first dream car. Also make sure that it allows warranty and maintenance and insurance and also services to be done or booked from this website. Also make sure we can give them best market prices and a smooth delivery process.

Screens I want: [list them, e.g. home, list of events, single event, contact]

Set it up with Next.js using the App Router, TypeScript, Tailwind and shadcn/ui.

No database and no login yet. Every screen reads from a mock data file. Put all data
access in src/data/ behind small named functions such as getEvents() and getEvent(id).
No screen or component may read the mock file directly. Keep the data shapes in
src/types.ts. We will replace src/data with Supabase in a later session, so keep that
folder swappable and keep everything else unaware of where the data comes from.

Plan it first. Show me the plan and the file structure, and wait for me to say go
before writing any code.
